/** Package authority fingerprint — bump when PAS registry contracts change materially. */
export const {{FINGERPRINT_CONST}} =
  "{{FINGERPRINT_VALUE}}" as const;

export const {{VERSION_CONST}} = "0.0.0" as const;
