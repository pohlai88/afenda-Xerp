# Slice B22 — Governance Import Boundary (PAS-002 §3.3)

**Prerequisite:** Slice B10 — architecture-authority skill (`docs/PAS/ARCHITECTURE-AUTHORITY/SLICE/b10-architecture-authority-skill.md`, `Status: Delivered`)

**Status:** Delivered (2026-06-27)

**Type:** Implementation

**Risk class:** Low

**Clean Core impact:** A→A — script import path only

## Purpose

Replace deep `packages/architecture-authority/src/validators/...` imports in governance scripts with approved package barrel imports (`@afenda/architecture-authority` or `@afenda/architecture-authority/surface` per surface registry rule).

## Handoff block

```
Handoff from: docs/PAS/ARCHITECTURE-AUTHORITY/SLICE/b22-3.3-governance-import-boundary.md

1. Objective    — Fix check-foundation-disposition.mts to import validateFoundationDisposition from @afenda/architecture-authority barrel; no deep src/ paths.
2. Allowed layer— scripts/governance/
3. Files        —
   scripts/governance/check-foundation-disposition.mts
   docs/PAS/ARCHITECTURE-AUTHORITY/SLICE/b22-3.3-governance-import-boundary.md
4. Prohibited   — packages/architecture-authority/src/** unless export missing (then stop and report); foundation-disposition.registry.ts
5. Authority    — PAS-002 §3.3 ┬À §4.11 import rules ┬À architecture-authority skill
6. Gates        —
   pnpm check:foundation-disposition
   pnpm --filter @afenda/architecture-authority typecheck
7. Closes       — Governance script deep-imports architecture-authority src
8. Evidence     — scripts/governance/check-foundation-disposition.mts
9. Attestation  — Governance ┬À Maintainability
```

## Rules frozen

1. Use workspace package import, not relative path into `packages/.../src/`.
2. If `validateFoundationDisposition` is not on root barrel, export it from index.ts in a minimal follow-up — prefer existing export.

## DoD

| # | Criterion | Gate |
| --- | --- | --- |
| 1 | No `packages/architecture-authority/src/` import in check script | ripgrep + gate run |
| 2 | `pnpm check:foundation-disposition` passes | CI script |
