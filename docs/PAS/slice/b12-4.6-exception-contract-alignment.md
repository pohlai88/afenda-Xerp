# Slice B12 — Exception Contract Alignment (PAS-002 §4.6)

**Prerequisite:** Slice B6 — exception registry (`docs/PAS/slice/b6-4.6-exception-registry.md`, `Status: Delivered`)

**Status:** Delivered (2026-06-27)

**Type:** Implementation

**Risk class:** Low

**Clean Core impact:** A→A — additive contract fields only; preserve existing adr/expiresAt fields

## Purpose

Align `ArchitectureException` with PAS-002 §4.6 governance fields (`id`, `status`, `owner`, `evidence`, `resolution?`) while retaining runtime fields (`adr`, `approvedBy`, `expiresAt`, `packageName`, `reason`, `subject`) for backward compatibility.

## Handoff block

```
Handoff from: docs/PAS/slice/b12-4.6-exception-contract-alignment.md

1. Objective    — Add PAS §4.6 governance fields to ArchitectureException; populate exception-registry.data.ts; extend validate-exceptions.ts; sync authority-surfaces.md exception section; add tests.
2. Allowed layer— packages/architecture-authority/src/contracts/ (+ data/, validators/, __tests__/) + .cursor/skills/architecture-authority/reference/authority-surfaces.md
3. Files        —
   packages/architecture-authority/src/contracts/exception.contract.ts
   packages/architecture-authority/src/data/exception-registry.data.ts
   packages/architecture-authority/src/validators/validate-exceptions.ts
   packages/architecture-authority/src/__tests__/validators-negative.test.ts
   .cursor/skills/architecture-authority/reference/authority-surfaces.md
   docs/PAS/slice/b12-4.6-exception-contract-alignment.md
4. Prohibited   — apps/erp; packages/kernel; foundation-disposition.registry.ts; breaking removal of adr/expiresAt fields
5. Authority    — PAS-002 §4.6 · §8 · architecture-authority skill
6. Gates        —
   pnpm --filter @afenda/architecture-authority typecheck
   pnpm --filter @afenda/architecture-authority test:run
   pnpm quality:architecture
7. Closes       — Exception contract/runtime drift vs PAS §4.6 and skill reference
8. Evidence     —
   packages/architecture-authority/src/contracts/exception.contract.ts
   packages/architecture-authority/src/__tests__/validators-negative.test.ts
9. Attestation  — Contract · Test · Documentation
```

## Rules frozen

1. Additive only — existing `adr`, `approvedBy`, `expiresAt`, `packageName`, `reason`, `subject` remain.
2. `status: "active"` requires non-empty `evidence` and valid `id`.
3. JSON-serializable readonly fields only.

## DoD

| # | Criterion | Gate |
| --- | --- | --- |
| 1 | Exception registry row includes id/status/owner/evidence | typecheck |
| 2 | Validator rejects active exception with empty evidence | test:run |
