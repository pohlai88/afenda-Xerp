# Slice B2 — Module Location (PAS-001 §4.1.2)

**Prerequisite:** Slice B1 — three-layer identity stack (`docs/PAS/PAS-001-KERNEL-AUTHORITY-STANDARD.md` §4.1.1, constitutional baseline)

**Status:** Delivered

**Type:** Implementation

**Risk class:** Low

**Clean Core impact:** A→A (layout contract only — no runtime behavior change)

## Purpose

Formalize PAS-001 §4.1.2 nested identity module layout: single source of truth for approved subfolders and family contract files, governance gate parity, and filesystem acceptance tests.

## Handoff block

```
Handoff from: docs/PAS/slice/b2-4.1.2-module-location.md

1. Objective    — Implement PAS-001 §4.1.2 kernel identity module location: typed layout contract, module-location tests, gate imports from contract constants, slice doc sync.
2. Allowed layer— packages/kernel (+ scripts/governance/check-kernel-identity-surface.mts, docs/PAS/)
3. Files        —
   packages/kernel/src/identity/governance/identity-module-layout.contract.ts
   packages/kernel/src/identity/governance/index.ts
   packages/kernel/src/identity/index.ts
   packages/kernel/src/identity/__tests__/module-location.test.ts
   packages/kernel/src/identity/__tests__/identity-boundary.test.ts
   scripts/governance/check-kernel-identity-surface.mts
   docs/PAS/slice/b2-4.1.2-module-location.md
4. Prohibited   — flat .ts files at identity root (except index.ts); duplicate platform-id*.ts paths; ID_FAMILIES registry edits; apps/erp; DB migrations; Accounting Core; external npm deps
5. Authority    — PAS-001 §4.1.2 · ADR-0021 · kernel-authority skill
6. Gates        —
   pnpm --filter @afenda/kernel typecheck
   pnpm --filter @afenda/kernel test:run
   pnpm check:kernel-identity-surface
   pnpm quality:boundaries
   pnpm quality:architecture
7. Closes       — Missing typed §4.1.2 layout contract; gate/test drift on folder allow-list
8. Evidence     —
   packages/kernel/src/identity/governance/identity-module-layout.contract.ts
   packages/kernel/src/identity/__tests__/module-location.test.ts
   scripts/governance/check-kernel-identity-surface.mts
9. Attestation  — Contract · Test · Governance · Documentation
```

## Rules frozen

1. Single source: `packages/kernel/src/identity/` — only `index.ts` at root.
2. Approved subfolders: `brand/`, `canonical/`, `registry/`, `families/`, `primitives/`, `tenant-human-reference/`, `postgres/`, `wire/`, `governance/`, `__tests__/`.
3. Approved `families/` files match PAS §4.1.4 category contract layout (seven files + barrel + define helper).
4. Legacy `contracts/platform-id*.ts` paths must remain absent.
5. `check:kernel-identity-surface` imports folder allow-lists from `identity-module-layout.contract.ts` — no duplicated literals.

## DoD

| # | Criterion | Gate |
| --- | --- | --- |
| 1 | Layout policy exported | module-location.test.ts |
| 2 | Only index.ts at identity root | module-location.test.ts |
| 3 | All approved subfolders on disk | module-location.test.ts |
| 4 | Family files match allow-list | module-location.test.ts |
| 5 | Retired platform-id paths absent | module-location.test.ts |
| 6 | Gate uses contract constants | check-kernel-identity-surface.mts |
| 7 | Kernel typecheck green | pnpm --filter @afenda/kernel typecheck |

## Runtime delivered

| Surface | Path |
| --- | --- |
| Layout contract | `identity/governance/identity-module-layout.contract.ts` |
| Policy export | `IDENTITY_MODULE_LAYOUT_POLICY` via `@afenda/kernel` |
| Filesystem tests | `identity/__tests__/module-location.test.ts` |
| Governance gate | `pnpm check:kernel-identity-surface` |
