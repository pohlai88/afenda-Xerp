# Slice B18 — Public Exports Parity (PAS-001 §6.3)

**Prerequisite:** Slice B16 — Target Package Structure (`docs/PAS/slice/b16-6.2-package-structure.md`, `Status: Delivered`)

**Status:** Delivered

**Type:** Evidence-sync

**Risk class:** Low

**Clean Core impact:** A→A — documentation sync and additive registry entries only; no runtime behavior change

---

## Objective

Sync PAS §6.3 document text from the stale six-key baseline to the delivered eight-key baseline, and complete `KERNEL_PACKAGE_TARGET_PATHS` with five B16 governance and permission contract files that exist on disk and are exported from their subpath barrels but were not yet registered.

1. Update PAS §6.3 JSON snippet from 6 subpaths to 8 (add `./permission` and `./governance`).
2. Update PAS §6.3 prose from "six keys — root plus five subpaths; delivered B3–B15" to "eight keys — root plus seven subpaths; delivered B3–B16".
3. Add 5 missing B16 paths to `KERNEL_PACKAGE_TARGET_PATHS`:
   - `packages/kernel/src/permission/permission-vocabulary.contract.ts`
   - `packages/kernel/src/governance/kernel-decision-matrix.contract.ts`
   - `packages/kernel/src/governance/kernel-contract-rules.policy.ts`
   - `packages/kernel/src/governance/kernel-runtime-rules.contract.ts`
   - `packages/kernel/src/governance/kernel-implementation-sequence.contract.ts`

---

## DoD

| # | Criterion | Evidence |
|---|-----------|----------|
| 1 | PAS §6.3 shows 8-key JSON (root + 7 subpaths) | `PAS-001-KERNEL-AUTHORITY-STANDARD.md §6.3` updated |
| 2 | PAS §6.3 prose references B16 and eight-key baseline | Prose text updated |
| 3 | `KERNEL_PACKAGE_TARGET_PATHS` has 21 entries | Contract file updated |
| 4 | `package-structure.test.ts` "lands every target path" passes with 21 entries | `pnpm --filter @afenda/kernel test:run` green |
| 5 | `check:kernel-package-structure` passes | Zero violations |
| 6 | `check:kernel-subpath-exports` passes | Zero violations |
| 7 | All acceptance gates pass | Typecheck + test + quality gates |

---

## Handoff block

```
1. Objective    — Sync PAS §6.3 doc from stale six-key to delivered eight-key baseline; add 5 missing B16 contract files to KERNEL_PACKAGE_TARGET_PATHS; create formal slice doc.
2. Allowed layer— packages/kernel/src/contracts/ + docs/PAS/ only
3. Files        — docs/PAS/PAS-001-KERNEL-AUTHORITY-STANDARD.md (MODIFY)
                  packages/kernel/src/contracts/kernel-package-layout.contract.ts (MODIFY)
                  docs/PAS/slice/b18-6.3-public-exports-parity.md (CREATE)
4. Prohibited   — apps/erp, packages/database, packages/ui, packages/kernel/package.json, check-kernel-subpath-exports.mts, subpath-exports.test.ts, ID_FAMILIES
5. Authority    — PAS-001 §6.3 / §6.4 — Package export governance
6. Gates        — pnpm --filter @afenda/kernel typecheck
                  pnpm --filter @afenda/kernel test:run
                  pnpm check:kernel-package-structure
                  pnpm check:kernel-subpath-exports
                  pnpm quality:boundaries
                  pnpm quality:architecture
7. Closes       — PAS §6.3 documentation drift; KERNEL_PACKAGE_TARGET_PATHS missing 5 B16 files
8. Evidence     — PAS §6.3 shows 8-key JSON; KERNEL_PACKAGE_TARGET_PATHS has 21 entries; all gates pass
9. Attestation  — Principal TypeScript Architect — Enterprise 9.5+/10
```

---

## Drift prevention

| Rule | Result |
|------|--------|
| No prohibited boundary crossed | Pass |
| No parallel registry created | Pass |
| No unauthorized package import | Pass |
| No raw `any` | Pass |
| No unsafe cast | Pass |
| No hand-edited migration | Pass |
| No dead code shipped | Pass |
| Wire normalizer coverage maintained | N/A |
