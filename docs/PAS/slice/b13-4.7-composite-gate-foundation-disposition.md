# Slice B13 — Composite Gate Foundation Disposition (PAS-002 §4.7)

**Prerequisite:** Slice B7 — architecture gates (`docs/PAS/slice/b7-4.7-architecture-gates.md`, `Status: Delivered`)

**Status:** Delivered (2026-06-27)

**Type:** Implementation

**Risk class:** Low

**Clean Core impact:** A→A — wire existing validator into composite gate only

## Purpose

`validateArchitecture` must include `validateFoundationDisposition()` so composite `pnpm quality:architecture` and `buildArchitectureReport` enforce foundation disposition integrity per PAS-002 §4.7 and `ValidationGate` union `"foundation-disposition"`.

## Handoff block

```
Handoff from: docs/PAS/slice/b13-4.7-composite-gate-foundation-disposition.md

1. Objective    — Wire validateFoundationDisposition into validateArchitecture composite results; add test proving disposition violations fail composite gate.
2. Allowed layer— packages/architecture-authority/src/validators/ (+ __tests__/, reports/ if report aggregation needs sync)
3. Files        —
   packages/architecture-authority/src/validators/validate-architecture.ts
   packages/architecture-authority/src/__tests__/validate-architecture.test.ts
   packages/architecture-authority/src/reports/build-architecture-report.ts
   docs/PAS/slice/b13-4.7-composite-gate-foundation-disposition.md
4. Prohibited   — foundation-disposition.registry.ts edits; apps/erp; packages/kernel; new npm deps; duplicate standalone check script removal
5. Authority    — PAS-002 §4.7 · §13.1 · architecture-authority skill
6. Gates        —
   pnpm --filter @afenda/architecture-authority typecheck
   pnpm --filter @afenda/architecture-authority test:run
   pnpm quality:architecture
   pnpm check:foundation-disposition
7. Closes       — Composite gate excludes foundation disposition; buildArchitectureReport incomplete validation
8. Evidence     —
   packages/architecture-authority/src/validators/validate-architecture.ts
   packages/architecture-authority/src/__tests__/validate-architecture.test.ts
9. Attestation  — Contract · Test · Governance
```

## Rules frozen

1. `validateFoundationDisposition()` runs without workspace input (same as standalone script).
2. Composite merge order preserves existing validators; append disposition last.
3. Do not remove `scripts/governance/check-foundation-disposition.mts` — CI may still call it directly.

## DoD

| # | Criterion | Gate |
| --- | --- | --- |
| 1 | `validateArchitecture` merges disposition result | `validate-architecture.test.ts` |
| 2 | All package tests pass | `pnpm --filter @afenda/architecture-authority test:run` |
