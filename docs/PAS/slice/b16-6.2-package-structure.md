# Slice B16 — Target Package Structure (PAS-001 §6.2 / §6.4)

**Prerequisite:** Slice B15 — execution context (`docs/PAS/slice/b15-4.3-execution-context.md`, `Status: Delivered`)

**Status:** Delivered

**Type:** Implementation

**Risk class:** Low

**Clean Core impact:** A→A (layout governance + type-surface normalization — no runtime behavior change)

## Purpose

Formalize PAS-001 §6.2 target kernel package structure and §6.4 subpath exports: typed layout contract, filesystem acceptance tests, governance gate parity, and removal of stale export drift.

## Handoff block

```
Handoff from: docs/PAS/slice/b16-6.2-package-structure.md

1. Objective    — Implement PAS-001 §6.2/§6.4 kernel package structure governance: layout contract constants, target-path filesystem tests, subpath export parity gate, public policy export, slice doc sync.
2. Allowed layer— packages/kernel (+ scripts/governance/check-kernel-package-structure.mts, package.json script alias, docs/PAS/)
3. Files        —
   packages/kernel/src/contracts/kernel-package-layout.contract.ts
   packages/kernel/src/__tests__/package-structure.test.ts
   packages/kernel/src/index.ts
   scripts/governance/check-kernel-package-structure.mts
   package.json
   docs/PAS/slice/b16-6.2-package-structure.md
4. Prohibited   — new subpath exports without contract registration; currency/fiscal context files; ID_FAMILIES edits; apps/erp; DB migrations; Accounting Core runtime; reintroducing contracts/platform-id*.ts
5. Authority    — PAS-001 §6.2 · §6.4 · kernel-authority/reference/package-structure.md · kernel-authority skill
6. Gates        —
   pnpm --filter @afenda/kernel typecheck
   pnpm --filter @afenda/kernel test:run
   pnpm check:kernel-package-structure
   pnpm quality:boundaries
   pnpm quality:architecture
7. Closes       — Missing typed §6.2 layout contract; missing subpath export parity gate; PAS §6.2 target paths unvalidated at CI
8. Evidence     —
   packages/kernel/src/contracts/kernel-package-layout.contract.ts
   packages/kernel/src/__tests__/package-structure.test.ts
   scripts/governance/check-kernel-package-structure.mts
9. Attestation  — Contract · Test · Governance · Documentation
```

## Rules frozen

1. Current tree is source truth; §6.2 lists additive target paths only.
2. Required target paths after approved slices: `problem-detail.contract.ts`, `localization-context.contract.ts`, `propagation/`, `events/`, `policy/`.
3. Prohibited paths: `context/currency-context.contract.ts`, `context/fiscal-calendar-context.contract.ts`.
4. Required subpath exports: `./context`, `./accounting-domain`, `./propagation`, `./events`, `./policy`, `./permission`, `./governance`.
5. Every registered subpath requires tests in `subpath-exports.test.ts` or `package-structure.test.ts`.
6. Legacy `contracts/platform-id*.ts` must remain absent.

## DoD

| # | Criterion | Gate |
| --- | --- | --- |
| 1 | Layout policy exported | package-structure.test.ts |
| 2 | All §6.2 target paths on disk | package-structure.test.ts |
| 3 | Prohibited §6.2 paths absent | package-structure.test.ts |
| 4 | package.json exports match contract | check-kernel-package-structure |
| 5 | Subpath smoke tests green | subpath-exports.test.ts |
| 6 | Kernel typecheck green | pnpm --filter @afenda/kernel typecheck |

## Runtime delivered

| Surface | Path |
| --- | --- |
| Layout contract | `contracts/kernel-package-layout.contract.ts` |
| Policy export | `KERNEL_PACKAGE_LAYOUT_POLICY` via `@afenda/kernel` |
| Filesystem tests | `__tests__/package-structure.test.ts` |
| Subpath smoke tests | `__tests__/subpath-exports.test.ts` |
| Governance gate | `pnpm check:kernel-package-structure` |
| Subpath export gate | `pnpm check:kernel-subpath-exports` (§6.4 dist parity) |
