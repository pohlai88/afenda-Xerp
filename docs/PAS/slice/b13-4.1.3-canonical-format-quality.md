# Slice B13 — Canonical Format Quality Pass (PAS-001 §4.1.3)

**Prerequisite:** Slice B7 — canonical ID validation rules (`docs/PAS/slice/b7-4.1.8.md`, `Status: Delivered`)

**Status:** Delivered

**Type:** Implementation

**Risk class:** Low

**Clean Core impact:** A→A (documentation and type-vocabulary alignment only — no behavioral change)

## Purpose

Quality-improve the PAS §4.1.3 canonical format implementation:

1. Delete the stale `canonical-enterprise-id-format.test.ts` — all six test cases are duplicated by the three newer test files authored in B7.
2. Add `@deprecated` JSDoc tags to `ANY_CANONICAL_ENTERPRISE_ID_PATTERN` and `ANY_CANONICAL_ENTERPRISE_ID_REGEX` so IDEs surface the migration signal.
3. Align `CanonicalId<TFamily>` generic constraint from `IdFamily` to `EnterpriseIdFamily` — same 22 keys, correct PAS vocabulary.
4. Fix step comment numbering in `parseCanonicalId` (1→2, 3→3, 4→4 — step 1 is `assertExactWireCanonicalForm`).

## Handoff block

```
Handoff from: docs/PAS/slice/b13-4.1.3-canonical-format-quality.md

1. Objective    — Quality-improve PAS §4.1.3 canonical format contracts: delete stale test file, add @deprecated JSDoc to compatibility aliases, align CanonicalId generic to EnterpriseIdFamily, fix step comment numbering in parseCanonicalId.
2. Allowed layer— packages/kernel/src/identity/canonical/ (+ docs/PAS/ doc sync)
3. Files        —
   packages/kernel/src/identity/canonical/__tests__/canonical-enterprise-id-format.test.ts (DELETE)
   packages/kernel/src/identity/canonical/canonical-id-format.contract.ts (MODIFY)
   packages/kernel/src/identity/canonical/canonical-id.contract.ts (MODIFY)
   packages/kernel/src/identity/canonical/canonical-id-parser.contract.ts (MODIFY)
   docs/PAS/slice/b13-4.1.3-canonical-format-quality.md (CREATE)
4. Prohibited   — ID_FAMILIES registry changes; DB migrations; apps/erp; external npm deps; changing canonical/index.ts exports
5. Authority    — PAS-001 §4.1.3 · ADR-0021 · kernel-authority skill
6. Gates        —
   pnpm --filter @afenda/kernel typecheck
   pnpm --filter @afenda/kernel test:run
   pnpm check:kernel-identity-surface
   pnpm quality:architecture
7. Closes       — Dead canonical-enterprise-id-format.test.ts; missing @deprecated markers on compatibility aliases; IdFamily vs EnterpriseIdFamily vocabulary drift
8. Evidence     —
   packages/kernel/src/identity/canonical/canonical-id-format.contract.ts
   packages/kernel/src/identity/canonical/canonical-id.contract.ts
   packages/kernel/src/identity/canonical/canonical-id-parser.contract.ts
   pnpm --filter @afenda/kernel test:run (all existing tests green; stale file removed)
9. Attestation  — Contract · Documentation · Cleanup
```

## DoD

| # | Criterion | Gate |
| --- | --- | --- |
| 1 | `canonical-enterprise-id-format.test.ts` deleted — test suite still green | `pnpm --filter @afenda/kernel test:run` |
| 2 | `ANY_CANONICAL_ENTERPRISE_ID_PATTERN` has `@deprecated` JSDoc | `canonical-id-format.contract.ts` inspection |
| 3 | `ANY_CANONICAL_ENTERPRISE_ID_REGEX` has `@deprecated` JSDoc | `canonical-id-format.contract.ts` inspection |
| 4 | `CanonicalId<TFamily extends EnterpriseIdFamily>` — PAS vocabulary aligned | `pnpm --filter @afenda/kernel typecheck` |
| 5 | Step comment numbering correct in `parseCanonicalId` (2, 3, 4) | `canonical-id-parser.contract.ts` inspection |
| 6 | Kernel typecheck green | `pnpm --filter @afenda/kernel typecheck` |
| 7 | Identity surface gate green | `pnpm check:kernel-identity-surface` |

## Risk register

| Risk | Status | Mitigation |
| --- | --- | --- |
| `IdFamily` → `EnterpriseIdFamily` breaks downstream | Low — same 22 keys | Typecheck proves equality |
| Deleting test file removes coverage | None — all cases covered by 3 newer files | Verified by test run |
