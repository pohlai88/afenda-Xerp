# Slice B57 — Kernel Permission Wire Triad (PAS-001 §8)

**Status:** Delivered

**Objective:** Wire permission model vocabulary (assert, parser, wire descriptor) in `@afenda/kernel` for ERP/metadata consumers.

**Evidence:** `packages/kernel/src/permission/`, `check-kernel-permission-wire-serializable` governance gate.
