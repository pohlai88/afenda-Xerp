/** Next.js `(auth)/error.tsx` segment boundary copy — user-safe, no raw errors. */
export const AUTH_SEGMENT_ERROR_COPY = {
  description:
    "The sign-in surface failed before your session could start. This is usually temporary — reload the page or wait a moment, then try again.",
  retryLabel: "Reload sign-in",
  title: "Could not load sign-in",
} as const;
