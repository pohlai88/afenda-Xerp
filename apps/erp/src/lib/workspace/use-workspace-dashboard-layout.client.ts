"use client";

/** ADR-0027 stub — appshell dashboard client removed; dev harness supplies layout locally. */
export function useWorkspaceDashboardLayout(): {
  readonly layout: null;
  readonly status: "idle";
} {
  return { layout: null, status: "idle" };
}
