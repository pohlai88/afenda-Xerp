---
title: "Environment variables reference"
noIndex: true
---

{/* DO NOT EDIT — generated by sync:product-docs */}

# Environment variables reference

Names and descriptions only — never commit secret values.

| Variable | Section | Description | Deprecated |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | .env.config (non-secret) | Supabase browser-safe | No |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | .env.config (non-secret) | — | No |
| `SUPABASE_JWT_KID` | .env.config (non-secret) | — | No |
| `SUPABASE_DB_REGION` | .env.config (non-secret) | — | No |
| `SUPABASE_DB_POOLER_HOST` | .env.config (non-secret) | — | No |
| `NEXT_PUBLIC_SENTRY_DSN` | .env.config (non-secret) | Derived automatically by `pnpm env:sync` (do not add to source files): `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_JWKS_URL`, `SUPABASE_PROJECT_REF` Used by ``@supabase/server`` (`API` routes / Edge Functions): `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `SUPABASE_JWKS_URL` Sentry public `DSN` | No |
| `STRIPE_DEFAULT_PLAN_KEY` | .env.config (non-secret) | Stripe price IDs (use `STRIPE_PRICE_PRO`, not legacy `STRIPE_PRICE_ID`) | No |
| `STRIPE_PRICE_PRO` | .env.config (non-secret) | — | No |
| `OBJECT_STORAGE_PROVIDER` | .env.config (non-secret) | Object storage (prefer `OBJECT_STORAGE_`* over legacy `CLOUDFLARE_R2_`*) | No |
| `OBJECT_STORAGE_BUCKET` | .env.config (non-secret) | — | No |
| `OBJECT_STORAGE_ENDPOINT` | .env.config (non-secret) | — | No |
| `BETTER_AUTH_IDENTIFY_URL` | .env.config (non-secret) | Better Auth Infrastructure KV identify endpoint (project-scoped; from dashboard) | No |
| `BETTER_AUTH_URL` | .env.config (non-secret) | Derived at sync: `BETTER_AUTH_KV_URL`, `NEXT_PUBLIC_BETTER_AUTH_KV_URL` Better Auth application instance (`ERP` origin) | No |
| `AFENDA_OAUTH_GOOGLE_CLIENT_ID` | .env.config (non-secret) | Platform OAuth client IDs (public; pair with secrets in .env.secret) | No |
| `AFENDA_OAUTH_GITHUB_CLIENT_ID` | .env.config (non-secret) | — | No |
| `ERP_CSP_STRATEGY` | .env.config (non-secret) | Transactional auth email (Resend) — from-address may live in .env.config or .env.secret `AFENDA_AUTH_EMAIL_FROM`=Afenda `ERP` `<`auth@yourdomain.com`>` `CSP` strategy: hybrid (public `SRI` + protected nonce), nonce, or sri | No |
| `UPSTASH_REDIS_REST_URL` | .env.config (non-secret) | Redis (Upstash `REST` — `URL` in config, token in secret) | No |
| `TRIGGER_PROJECT_REF` | .env.config (non-secret) | Trigger.dev (`@afenda/execution` — project ref + `API` `URL` in config; secret in .env.secret) | No |
| `TRIGGER_API_URL` | .env.config (non-secret) | — | No |
| `OUTBOX_SCHEDULER_REQUIRED` | .env.config (non-secret) | Execution spine policy (enqueue vs scheduled drain) Local default: `OUTBOX_SCHEDULER_REQUIRED`=false (degraded — no remote cron registration) Local spine profile: `OUTBOX_SCHEDULER_REQUIRED`=true + `TRIGGER_SECRET_KEY`=tr_dev_* Production overlay (.env.config.production): `OUTBOX_SCHEDULER_REQUIRED`=true | No |
| `WORKER_RELEASE_CHECK_REQUIRED` | .env.config (non-secret) | — | No |
| `ALLOW_DEGRADED_EXECUTION` | .env.config (non-secret) | — | No |
| `API_RATE_LIMIT_PROVIDER` | .env.config (non-secret) | Governed internal v1 `REST` rate limiting — `memory` for local dev; omit in production for Postgres buckets | No |
| `BETTER_AUTH_SECRET` | .env.secret | Better Auth signing secret (min 32 chars — generate with: openssl rand -base64 32) | No |
| `AFENDA_INTERNAL_S2S_SIGNING_SECRET` | .env.secret | Machine `S2S` internal v1 `REST` (`ADR`-0035/0036 — apps/erp only; pair with service-token-required routes) Required when deploying `GET` /api/internal/v1/auth/service-actor/ping or any service-token route. Issuers (workers, probes) and `ERP` must share this secret. Not used for human OAuth/session auth. | No |
| `AFENDA_OAUTH_GOOGLE_CLIENT_SECRET` | .env.secret | Platform OAuth client secrets (pair with `AFENDA_OAUTH_`*_CLIENT_ID in .env.config) | No |
| `AFENDA_OAUTH_GITHUB_CLIENT_SECRET` | .env.secret | — | No |
| `AFENDA_DEV_LOGIN_PASSWORD` | .env.secret | Local dev sign-in (after pnpm db:bootstrap:local → pnpm auth:bootstrap:dev) Email defaults to `dev-admin@localhost.afenda`; override with `AFENDA_DEV_LOGIN_EMAIL` | No |
| `AFENDA_E2E_VIEWER_LOGIN_PASSWORD` | .env.secret | Optional override for `E2E` workspace_reader (defaults to \${`AFENDA_DEV_LOGIN_PASSWORD`}-viewer after auth:bootstrap:dev) | No |
| `BETTER_AUTH_API_KEY` | .env.secret | System Admin diagnostics — set to 1 to run full delegated Phase 9 gates on every page load (slow). Default page load uses structure-only; prefer the in-app "Run full delegated gate check" action instead. `AFENDA_ACCOUNTING_READINESS_DIAGNOSTICS_FULL`=1 Better Auth Infrastructure (dashboard / dash / sentinel plugins) | No |
| `AFENDA_AUTH_EMAIL_API_KEY` | .env.secret | Resend transactional email (auth verification, password reset, member invitations, 2FA `OTP`) | No |
| `AFENDA_RESEND_WEBHOOK_SECRET` | .env.secret | `AFENDA_AUTH_EMAIL_FROM`=Afenda `ERP` `<`auth@yourdomain.com`>` Passwordless 2FA scope — credential-only (default) \| enforce-all (requires future Better Auth hooks; no-op today) `AFENDA_AUTH_PASSWORDLESS_TWO_FACTOR`=credential-only | No |
| `SUPABASE_DB_PASSWORD` | .env.secret | Resend developer `MCP` only (Cursor resend-dev server — `NOT` `ERP` runtime / tenant mail) `RESEND_API_KEY`=re_[your-resend-dev-or-sandbox-api-key] `RESEND_FROM`=`onboarding@resend.dev` Supabase Postgres (Afenda builds connection URLs from password + region + project ref) | No |
| `SUPABASE_SECRET_KEY` | .env.secret | — | No |
| `UPSTASH_REDIS_REST_TOKEN` | .env.secret | — | No |
| `OBJECT_STORAGE_ACCESS_KEY_ID` | .env.secret | Optional explicit Postgres overrides (otherwise built at runtime): `DATABASE_URL`                 — explicit app override `DATABASE_URL_DEDICATED`       — dedicated pooler (db.[ref].supabase.co:6543) `DATABASE_URL_DIRECT`          — migrations; prefer session pooler when direct host fails locally: postgresql://postgres.[ref]:[password]@[`SUPABASE_DB_POOLER_HOST`]:5432/postgres `DATABASE_URL_TRANSACTION`       — shared transaction pooler (port 6543, app default) `DATABASE_URL_SESSION`           — shared session pooler (port 5432) | No |
| `OBJECT_STORAGE_SECRET_ACCESS_KEY` | .env.secret | — | No |
| `STRIPE_SECRET_KEY` | .env.secret | — | No |
| `STRIPE_WEBHOOK_SECRET` | .env.secret | — | No |
| `TURBO_TEAM` | .env.secret | Turborepo remote cache (secret only — do not duplicate `TURBO_TEAM` in .env.config) | Yes |
| `TURBO_TOKEN` | .env.secret | — | No |
| `SHADCN_STUDIO_LICENSE_KEY` | .env.secret | shadcn/studio `MCP` — license from https://shadcnstudio.com/mcp/onboarding Synced to .env for .cursor/mcp.json → shadcn-studio | No |
| `SHADCN_STUDIO_ACCOUNT_EMAIL` | .env.secret | — | No |
| `TRIGGER_SECRET_KEY` | .env.secret | Figma — auth shell design file (optional; used in Figma `MCP` / /ftc prompts) `FIGMA_AUTH_DESIGN_URL`=https://www.figma.com/design/2ZNqNOxyNb5TwCTBIaMVPD/loginauth Trigger.dev `DEV` secret key (cloud.trigger.dev → `API` Keys; use tr_prod_* in production) | No |
| `TRIGGER_SECRET_KEY_PROD` | .env.secret | Production-only Trigger key — merged over `TRIGGER_SECRET_KEY` when pushing Vercel production overlay | No |
| `DOCS_GITHUB_TOKEN` | .env.secret | `@afenda/docs` — GitHub `REST` token for last-modified page footer during production builds Fine-grained or classic token with contents:read on afenda-Xerp; optional (raises `API` rate limits) | No |
