---
title: "System Admin areas"
description: "Tenant administration sections with routes and read permissions — synced from the product catalog."
audience: tenant-admin
catalogBindings:
  - system-admin
---

{/* DO NOT EDIT — generated by sync:product-docs / generate-feature-evidence */}

import { Callout } from "fumadocs-ui/components/callout";

## Overview

System Admin is where tenant administrators manage users, memberships, roles, permissions, and audit visibility. This page lists governed admin areas and the read permission each surface expects.

## When to use

Use this map when onboarding a new tenant admin or verifying why a navigation item is hidden. Pair with [Users and memberships](/docs/configure-tenant/users-and-memberships) and [Roles and permissions](/docs/configure-tenant/roles-and-permissions).

## Admin areas

| Area | Route | Read permission | Summary |
| --- | --- | --- | --- |
| Audit | `/system-admin/audit` | `system_admin.audit_read` | พื้นที่ System Admin สำหรับ audit — ต้องมี `system_admin.audit_read` |
| Diagnostics | `/system-admin/diagnostics` | `system_admin.audit_read` | พื้นที่ System Admin สำหรับ diagnostics — ต้องมี `system_admin.audit_read` |
| Memberships | `/system-admin/memberships` | `system_admin.users_read` | พื้นที่ System Admin สำหรับ memberships — ต้องมี `system_admin.users_read` |
| Permissions | `/system-admin/permissions` | `system_admin.permissions_manage` | พื้นที่ System Admin สำหรับ permissions — ต้องมี `system_admin.permissions_read` |
| Roles | `/system-admin/roles` | `system_admin.roles_manage` | พื้นที่ System Admin สำหรับ roles — ต้องมี `system_admin.roles_read` |
| Settings | `/system-admin/settings` | `system_admin.modules_manage` | พื้นที่ System Admin สำหรับ settings — ต้องมี `system_admin.settings_read` |
| Users | `/system-admin/users` | `system_admin.users_read` | พื้นที่ System Admin สำหรับ users — ต้องมี `system_admin.users_read` |

<Callout type="info" title="ผู้ดูแล tenant">

For the full permission matrix, see [Permissions reference](/docs/configure-tenant/generated/permissions).

</Callout>
