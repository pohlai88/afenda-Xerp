---
title: "System Admin areas"
description: "Tenant administration sections with routes and read permissions — synced from the product catalog."
audience: tenant-admin
catalogBindings:
  - system-admin
---

{/* DO NOT EDIT — generated by sync:product-docs / generate-feature-evidence */}

import { Callout } from "fumadocs-ui/components/callout";

## Overview

System Admin is where tenant administrators manage users, memberships, roles, permissions, and audit visibility. This page lists governed admin areas and the read permission each surface expects.

## When to use

Use this map when onboarding a new tenant admin or verifying why a navigation item is hidden. Pair with [Users and memberships](/docs/configure-tenant/users-and-memberships) and [Roles and permissions](/docs/configure-tenant/roles-and-permissions).

## Admin areas

| Area | Route | Read permission | Summary |
| --- | --- | --- | --- |
| Audit | `/system-admin/audit` | `system_admin.audit_read` | System Admin area for audit — requires `system_admin.audit_read`. |
| Diagnostics | `/system-admin/diagnostics` | `system_admin.audit_read` | System Admin area for diagnostics — requires `system_admin.audit_read`. |
| Memberships | `/system-admin/memberships` | `system_admin.users_read` | System Admin area for memberships — requires `system_admin.users_read`. |
| Permissions | `/system-admin/permissions` | `system_admin.permissions_manage` | System Admin area for permissions — requires `system_admin.permissions_manage`. |
| Roles | `/system-admin/roles` | `system_admin.roles_manage` | System Admin area for roles — requires `system_admin.roles_manage`. |
| Settings | `/system-admin/settings` | `system_admin.modules_manage` | System Admin area for settings — requires `system_admin.modules_manage`. |
| Users | `/system-admin/users` | `system_admin.users_read` | System Admin area for users — requires `system_admin.users_read`. |

<Callout type="info" title="Need deeper reference?">

For the full permission matrix, see [Permissions reference](/docs/configure-tenant/generated/permissions).

</Callout>
