# Storybook lab public assets

Lab-only static files served at `/` root in Storybook (e.g. favicons, fixture JSON).

Block promotion assets belong in `packages/shadcn-studio/public/` (mounted at `/studio-assets/`).
