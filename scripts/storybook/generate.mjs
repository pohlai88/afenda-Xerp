/**
 * Storybook codegen — disabled while the lab catalog is empty.
 *
 * Re-enable: restore GENERATORS loop + gates in git history, set stories globs in
 * apps/storybook/.storybook/main.ts, then run `pnpm storybook generate`.
 */
console.log(
  "storybook:generate — skipped (empty Storybook lab). Restore generators in scripts/storybook/generate.mjs when rebuilding the catalog."
);
