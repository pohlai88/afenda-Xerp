diff --git a/packages/shadcn-studio-v2/AGENTS.md b/packages/shadcn-studio-v2/AGENTS.md
index a83e2a34..c75d9cb5 100644
--- a/packages/shadcn-studio-v2/AGENTS.md
+++ b/packages/shadcn-studio-v2/AGENTS.md
@@ -4,7 +4,8 @@ This file governs work inside `packages/shadcn-studio-v2` only.
 
 ## Purpose
 
-Use this package as the clean V2 target for structure, naming, and export-boundary discipline.
+Use this package as the clean V2 target for structure, naming, export-boundary
+discipline, and code-first package execution.
 
 This file exists to keep V2 local decisions tight, explicit, and separate from:
 
@@ -32,19 +33,54 @@ This contract does not govern:
 
 When working in this package, follow authority in this order:
 
-1. `docs/TAXONOMY.md`
-2. this `AGENTS.md`
-3. repo-root `AGENTS.md`
-4. active repo rules under `.cursor/rules/`
-5. active repo skills under `.cursor/skills/`
+1. `docs/DESIGN-SYSTEM-ARCHITECTURE.md`
+2. `docs/TAXONOMY.md`
+3. `docs/DEVELOPMENT-ROADMAP.md`
+4. this `AGENTS.md`
+5. repo-root `AGENTS.md`
+6. active repo rules under `.cursor/rules/`
+7. active repo skills under `.cursor/skills/`
 
-If a lower source conflicts with `docs/TAXONOMY.md`, `docs/TAXONOMY.md` wins for V2 structure.
+Use the authority stack by concern:
+
+- `docs/DESIGN-SYSTEM-ARCHITECTURE.md` owns the code-first package model,
+  public-import law, documentation reduction rule, and acceptance standard.
+- `docs/TAXONOMY.md` owns allowed structure, naming, and root `src/` boundary
+  files.
+- `docs/DEVELOPMENT-ROADMAP.md` owns phase order, slice order, and required
+  proof sequence.
+
+If a lower source conflicts with one of those documents, the higher document
+wins within its own concern area.
 
 Use these local package documents together:
 
 - `docs/TAXONOMY.md` for structure and naming authority
-- `docs/ROADMAP.md` for slice sequencing and gates
+- `docs/DESIGN-SYSTEM-ARCHITECTURE.md` for package operating law and public
+  boundary rules
+- `docs/DEVELOPMENT-ROADMAP.md` for slice sequencing and gates
 - `docs/MIGRATION-MAP.md` for legacy-to-V2 translation tracking
+- `docs/slices/*` for active execution-facing technical specs
+
+## Active Docs Boundary
+
+The active documentation lane for V2 is intentionally small.
+
+Treat these as active package docs:
+
+- `docs/DESIGN-SYSTEM-ARCHITECTURE.md`
+- `docs/DEVELOPMENT-ROADMAP.md`
+- `docs/TAXONOMY.md`
+- `docs/README.md`
+- `docs/MIGRATION-MAP.md`
+- `docs/DESIGN-SYSTEM-GUIDELINE.md`
+- `docs/PRIMITIVE-API-CONSISTENCY.md`
+- `docs/LEGACY-RETIREMENT-PLAN.md`
+- `docs/slices/*`
+
+Treat `docs/archive/**` as historical material only. It may inform recovery,
+migration, or refactoring context, but it is not live implementation authority
+unless one of the active docs explicitly pulls a rule forward.
 
 ## Structural Law
 
@@ -101,6 +137,11 @@ These are public API boundary files, not casual implementation files.
 
 Do not add more root export files unless `docs/TAXONOMY.md` is amended first.
 
+Package export entrypoints are broader than root `src/` files. The package may
+also expose approved subpath exports such as `@afenda/shadcn-studio-v2/theme`
+and package-exported CSS paths, but those exports must resolve from approved
+boundaries rather than from new root `src/` files.
+
 ## Migration Discipline
 
 V2 adopts the reference philosophy and does not copy the reference app tree literally.
@@ -167,7 +208,8 @@ Keep V2 narrow, explicit, and migration-safe.
 
 ## Roadmap Discipline
 
-V2 follows the ordered slice model in `docs/ROADMAP.md`.
+V2 follows the ordered slice model in `docs/DEVELOPMENT-ROADMAP.md` and the
+active execution specs in `docs/slices/*`.
 
 Hard rule:
 
