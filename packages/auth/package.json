{
  "name": "@afenda/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Authentication foundation (Better Auth in TIP-004)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./client": {
      "types": "./dist/auth.client.d.ts",
      "import": "./dist/auth.client.js",
      "default": "./dist/auth.client.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.vitest.json --noEmit",
    "test": "vitest --config vitest.config.ts",
    "test:run": "vitest run --config vitest.config.ts",
    "auth:bootstrap:dev": "tsx scripts/bootstrap-dev-login.ts",
    "clean": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true })\""
  },
  "sideEffects": false,
  "peerDependencies": {
    "next": "catalog:",
    "react": "catalog:"
  },
  "devDependencies": {
    "@afenda/typescript-config": "workspace:*",
    "dotenv": "^17.4.2",
    "next": "catalog:",
    "react": "catalog:",
    "tsx": "^4.20.3",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@afenda/database": "workspace:*",
    "@afenda/kernel": "workspace:*",
    "@better-auth/passkey": "^1.6.20",
    "@better-auth/sso": "^1.6.20",
    "better-auth": "^1.6.20",
    "drizzle-orm": "^0.45.2"
  }
}
