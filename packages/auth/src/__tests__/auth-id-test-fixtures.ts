import { createTestEnterpriseId } from "@afenda/kernel";

/** Canonical platform user id for auth contract tests. */
export const AUTH_TEST_PLATFORM_USER_ID = createTestEnterpriseId(
  "user",
  "01ARZ3NDEKTSV4RRFFQ69G5FAV"
);
