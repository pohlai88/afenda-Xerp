{
  "name": "@afenda/entitlements",
  "version": "0.0.0",
  "private": true,
  "description": "Feature flags, entitlements, limits, beta access, localization, and kill-switch foundation",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:governance": "vitest run --config vitest.config.ts src/__tests__/index.test.ts",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.vitest.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "clean": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true })\""
  },
  "sideEffects": false,
  "dependencies": {
    "@afenda/database": "workspace:^",
    "@upstash/redis": "^1.35.3"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
